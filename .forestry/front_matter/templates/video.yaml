---
label: Video
hide_body: true
fields:
  - type: file
    name: source
    label: Video
  - type: file
    name: cover
    label: Video Cover Image
    description: The cover image displayed when the video is loading. Only used if no slide photo is set.

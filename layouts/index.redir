{{ if .Site.Data.redirects.site_redirects }}
    {{ range .Site.Data.redirects.site_redirects }}
        {{ if and .redirect.from .redirect.to }}
            {{ .redirect.to }}&emsp;{{ .redirect.from }}<br />
        {{ end }}
    {{ end }}
{{ end }}

{{- range $p := .Site.Pages -}}
{{- range .Aliases }}
{{ . }} {{ $p.RelPermalink -}}
{{- end }}
{{- end -}}

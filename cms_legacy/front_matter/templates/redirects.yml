---
label: Redirects
hide_body: true
fields:
- name: site_redirects
  type: field_group_list
  fields:
  - name: from
    type: text
    config:
      required: false
    label: From
  - name: to
    type: text
    config:
      required: false
    label: To
  config:
    min: 
    max: 
    labelField: 
  label: Site Redirects
pages:
- data/redirects.yaml
